
// Write your code here

// Provided variables (you may define these if not already defined)
const num1 = 2;
const num2 = 31;
const num3 = 16;
const num4 = 6;

// 1. Multiply to get 62
const multiply = num1 * num2; // 2 * 31 = 62

// 2. Generate a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1; // Any range above 0 works

// 3. Calculate the remainder to get 4
const mod = num3 % num4; // 16 % 6 = 4

// 4. Find the highest number in a set
const max = Math.max(5, 10, 15, 20); // 20 is the highest
